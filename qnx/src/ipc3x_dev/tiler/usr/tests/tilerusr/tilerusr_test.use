TilerUsr Test for Tiler Usr API

Syntax:
  tilerusr_test [<range>]

  <range>               empty:   run all tests
                        list:    list tests
                        ix:      run test #ix
                        a ..:    run tests #a, #a+1, ...
                        .. b:    run tests #1, #2, .. #b
                        a .. b:  run tests #a, #a+1, .. #b

Examples:
  tilerusr_test           Runs all tests
  tilerusr_test list      Lists all the tests
  tilerusr_test 1         Run test number 1
  tilerusr_test 10 ..     Run tests 10 though the end
  tilerusr_test .. 10     Run tests 1 through 10
  tilerusr_test 10 .. 20  Run tests 10 through 20
